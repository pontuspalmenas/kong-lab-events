_format_version: "3.0"

routes:
  - name: kafka-poll
    paths:
      - /kafka/poll
    plugins:
      - name: kafka-consume
        config:
          topics:
            - name: kong-events
          bootstrap_servers:
            - host: kafka
              port: 9093
          security:
            certificate_id: placeholder
            ssl: true